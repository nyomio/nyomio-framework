apiVersion: apps/v1
kind: Deployment
metadata:
  name: "admin-deployment"
spec:
  selector:
    matchLabels:
      app: "admin"
  revisionHistoryLimit: 1
  template:
    metadata:
      labels:
        app: "admin"
    spec:
      containers:
        - name: "admin"
          image: "admin:v1"
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: 8080
            - name: debug
              containerPort: 8000
          env:
            - name: JWT_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: nyom-apps
                  key: jwtsecret
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: nyom-db-postgresql
                  key: postgresql-password
            - name: DB_USER
              value: postgres
            - name: DB_URL
              value: jdbc:postgresql://nyom-db-postgresql.default.svc.cluster.local:5432/postgres
---
apiVersion: v1
kind: Service
metadata:
  name: "admin-service"
spec:
  selector:
    app: "admin"
  ports:
    - name: http
      targetPort: 8080
      port: 8080
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: admin-ingress
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`app.nyomio.local`) && PathPrefix(`/api/v1/admin/`)
      kind: Rule
      priority: 2
      services:
        - name: admin-service
          port: 8080
  tls:
    secretName: certsecret
